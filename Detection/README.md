# Детектирование

## Темы

- [Современные модели для сегментации](SAM.ipynb)
- [История детекторов](RCNN.ipynb)
- [Loss для детектора](RetinaNet.ipynb)
- [mAP - метрика для оценки детекторов](mAP.ipynb)
- [YOLO](YOLO.ipynb)
- [Open-World Localization](OWL_ViT.ipynb)
- [DINO + SAM](DINO_SAM.ipynb)

## Современные модели для сегментации

После
появления моделей на основе трансформерных
архитектур их  стали применять для
решения многих задач компьютерного
зрения. Классический ViT плохо подходит
для выделения точных границ объектов.
Так как изображение превращается в
последовательность путём нарезки на
фрагменты большого размера (16x16).

Что бы сохранить информацию о пиксельных
границах используют иерархические
архитектуры с меньшим размером patch и
локальным self-attention.

Примером такой архитектуры является[SWIN](https://arxiv.org/abs/2103.14030).

На базе иерархической трансформерной архитектуры построена модель SegFormer которая решает задачу попиксельной сегментации так же как и классические модели.

Затем появилась [MaskFormer](https://arxiv.org/abs/2107.06278) архитектуры. Принципиальным их отличием является то что предсказывается не класс отдельного пикселя а сразу маска класса.

В модели есть блок который предсказывает
вектор признаков соответствующий классу
(размерность С), а пиксельный декодер
возвращает  пространственную карту
признаков (CxHxW). Их перемножение позволяет
получить маску класса. Такая архитектура
позволяет решать задачи Instance и Panoptic
Segmentation. Дальнейшим развитием идеи стала
модель [OneFormer](https://arxiv.org/abs/2211.06220)
способная решать любую из этих задач.

Тип задачи задаётся текстовым промптом.

Модели семейства Segment Anything ([SAM](https://arxiv.org/abs/2304.02643)/[SAM2](https://arxiv.org/abs/2408.00714)) используют подобную архитектуру и позволяют использовать разные типы промпта.

Например координаты точки на изображении, текст или bounding box.

[Презентация](https://docs.google.com/presentation/d/19tKhAVJubedsiTjoJkLjjsT0YnzyOnq_QRJGz64w4Rw/edit#slide=id.g3355bc1050f_0_175)

Материалы: [SAM.ipynb](SAM.ipynb), [presentation](https://docs.google.com/presentation/d/1Z6K-GqKVCSfhOVYasbRySO-2QPcQkrvpqsrNtq01yeY/edit?slide=id.g3355bc1050f_0_175#slide=id.g3355bc1050f_0_175)

## История детекторов

**Чтобы** лучше понять работу современных детекторов, полезно знать, как они эволюционировали.

Сначала для поиска регионов, в которых может быть расположен объект (ROI — Region Of Interest), использовалась эвристика. Для каждого ROI запускалась нейросеть, предсказывающая: а) класс объекта; б) смещение ROI (xyxy) относительно исходного положения, выданного эвристикой (RCNN).

Чтобы ускорить процесс, ROI стали проецировать на карту признаков и делать предсказание уже на признаках (features) (Fast-RCNN). Для проекции использовались слои **ROI Pooling**, а затем **ROI Align**.

Эвристику заменили на отдельную нейросеть, которая предсказывала ROI сразу на признаках (Faster-RCNN).

Позже нейросеть для предсказания ROI убрали, заменив её фиксированным набором прямоугольников (~10K) (One Shot Detector).

...

Материалы: [RCNN.ipynb](RCNN.ipynb), [presentation](https://docs.google.com/presentation/d/1RWFG3ZTNp6N4WEvwLdjQQpm0kWJRtQUB_quq5eVvJC4/edit?slide=id.g3bdf6837da1_0_115#slide=id.g3bdf6837da1_0_115)

## Loss для детектора

Лосс функция для детекторов включает две компоненты: регрессионную (MSE) оценивающую качество предсказания координат ограничивающих прямоугольников (bounding box) и классификационную (CE) - отвечающую за предсказания класса объекта внутри bounding box. Суммарный лосс это их взвешенная сумма: **Loss = L_class + aL_reg**

Для классических детекторов проблемой тако лосс функции является наличие огромного количества bounding box которы не попадают на объект. Поэтому bbox у которых пресечение с GT(истинный bbox из разметки) исключаются из подсчета лосс.

Другой проблеммой является сильный дисбалланс классов: как правило большая часть пикселей изображения относиться к классу background. И даже если ошибка модели для этих пикселей невелика, то сумма этих небольших ошибок становиться значительной.

Что бы нивелировать их влияние используют FocalLoss

Материалы: [RetinaNet.ipynb](RetinaNet.ipynb), [giou.stanford.edu](https://giou.stanford.edu/), [generalized_intersection_over_union](https://lightning.ai/docs/torchmetrics/stable//detection/generalized_intersection_over_union.html), [abs](https://arxiv.org/abs/2006.04388)

## mAP - метрика для оценки детекторов

Чтобы подсчитать классификационные метрики (Precision, Recall, F1-Score), основанные на подсчёте ошибок I и II рода (TP, FP, FN, TN), необходимо выбрать способ сопоставления предсказанных моделью bbox с bbox из GT (Ground Truth).

При расчёте mAP задаётся порог IoU, и все bbox'ы с уровнем пересечения ниже этого порога не считаются ошибками. На основе такой разметки стандартным образом строится PR-кривая (Precision-Recall Curve).

Затем сглаживаем её (красный пунктир). Площадь под этой кривой и будет значением метрики AP (Average Precision). Если повторить эту процедуру для всех классов и усреднить, получим значение mAP (mean Average Precision) — метрику, которая оценивает качество детектирования для выбранного порога по IoU.

Материалы: [mAP.ipynb](mAP.ipynb)

## YOLO

YOLO (You Look Only Once) - название популярного One Shot детектора. Под этой аббривеатурой выпощено более десятка архитектур с различными backbone.

Основной причиной их популярности является скорость: это детекторы способные работать в реальном времени.

[YOLO Evolution: A Comprehensive Benchmark and Architectural Review of YOLOv12, YOLO11, and Their Previous Versions](https://arxiv.org/html/2411.00201v3)

Материалы: [YOLO.ipynb](YOLO.ipynb)

## Open-World Localization

Модели семейства YOLO/RCNN обучаются детектировать объекты из фиксированного списка классов. Существуют модели для детектирования/сегментации позволяющие осуществлять поиск объекта на изображении по произвольному текстовому запросу.

Примером такой модели может служить: [OWL-ViT](https://arxiv.org/abs/2205.06230)/[OWL-ViT2](https://arxiv.org/abs/2306.09683), [YOLO-World,](https://github.com/AILab-CVC/YOLO-World)[Grounding DINO](https://github.com/IDEA-Research/GroundingDINO)

Текст преобразовывается в embedding при помощи CLIP - like алгоритма, затем

сравнивается с эмбеддингами bbox

Материалы: [OWL_ViT.ipynb](OWL_ViT.ipynb)

## DINO + SAM

Эта связка позволяет автоматически находить и выделять объекты на изображении **без ручной разметки** и **без предварительного обучения на конкретной задаче** — всё работает "из коробки", благодаря возможностям современных foundation-моделей.

Как они работают вместе?

[**Grounding DINO**](https://github.com/IDEA-Research/GroundingDINO) получает на вход изображение и текстовый запрос (например, *cat*, *person*, *car*) и возвращает **bounding boxes** — области изображения, которые с высокой вероятностью соответствуют запрошенному объекту.

[**SAM**](https://github.com/facebookresearch/segment-anything) использует эти bounding boxes как подсказки и на их основе строит **маски сегментации**, отделяя найденные объекты от фона на уровне пикселей.

Материалы: [DINO_SAM.ipynb](DINO_SAM.ipynb), [SAM.ipynb](SAM.ipynb)
